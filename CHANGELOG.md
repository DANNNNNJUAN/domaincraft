# 更新记录

简体中文 · [English](CHANGELOG.en.md)

## 0.1.1 — 2026-09-22

修复 `check.py` 在正常包布局中的模块命名，以及未知依赖被静默略过的问题。

- 新增 `import_roots`，把源码路径映射为真实 Python 包名，支持包内相对导入、`src/` 和命名空间包。
- 新增 `--model-file` 和 `--architecture-file`，配置可以留在包内或文档目录。
- 未知导入、扫描范围外的本地依赖、空层匹配、动态导入和星号导入均使架构检查失败。
- 第三方导入通过 `external` 显式声明；标准库默认识别，禁止依赖仍优先执行。
- 报告增加 `architecture_graph` 和逐条 `architecture_imports`，保留规则追溯与注释检查。
- 增加 18 项包布局和失败行为回归，工具回归共 87 项。

迁移已有配置时，请核对包前缀并声明外部依赖。旧版本因未解析导入而通过的结果，需要重新验收。

## 0.1.0 — 2026-09-22

DomainCraft 的首个预览版，提供从领域设计到本地验收的一套 Python 工作流程。

- 用业务规则连接对象职责、SOLID 评审和模式选择。
- 检查模型关联、依赖、注释和测试执行结果，附带内存与 SQLite 订单示例。
- 支持多源码根、包边界、循环依赖，以及显式声明的运行时和共享数据依赖。
- 保存重构基线，接入原生测试和临时服务，按修改范围与具体目标给出 PASS/FAIL。
- 提供本地安装器、可重复构建的发布包、文件摘要、干净安装验证和 GitHub Actions。
- 提供完整中英文文档，包含实际改造前后样例。

当前环境与能力范围见[验证记录](docs/validation.md)。
